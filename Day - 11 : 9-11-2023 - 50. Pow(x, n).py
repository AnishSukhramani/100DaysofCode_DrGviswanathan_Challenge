class Solution(object):
    def myPow(self, x, n):
        return x**n

# This is a simple Python class with a method called `myPow`. It takes two parameters, `x` and `n`, and returns the result of raising `x` to the power of `n` using the `**` operator.
